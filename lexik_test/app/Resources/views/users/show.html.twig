{% extends 'base.html.twig' %}
{% block body %}

    <div class="jumbotron jumbotron-fluid">
        <div class="container">
            <div class="form-group">
                <h4 class="" style="color: #ac2925">Nom : </h4>
                <p class="lead">{{ user.name|capitalize }}</p>
            </div>
            <div class="form-group">
                <h4 class="" style="color: #ac2925">Prénom : </h4>
                <p class="lead">{{ user.lastname|capitalize }}</p>
            </div>
            <div class="form-group">
                <h4 class="" style="color: #ac2925">Date de naissance : </h4>
                <p class="lead">{{ user.birthday|date('Y-m-d') }}</p>
            </div>
            <div class="form-group">
                <h4 class="" style="color: #ac2925">Adresse e-mail : </h4>
                <p class="lead">{{ user.email }}</p>
            </div>
            <div class="form-group">
                <h4 class="" style="color: #ac2925">Groupe associer : </h4>
                <p class="lead">{{ user.groups|capitalize }}</p>
            </div>
            <div class="form-group">
                <a href="{{ path('users_edit', { 'id': user.id }) }}" class="btn btn-warning" style="float: left;margin-right: 10px"> Modifier</a>
            </div>
        </div>
    </div>

{% endblock %}
