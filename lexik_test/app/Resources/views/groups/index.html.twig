{% extends 'base.html.twig' %}

{% block body %}

    <div class="" style="margin-bottom: 30px;margin-top: 100px">
        <h1>Liste des groupes </h1>
        <a href="{{ path('groups_new') }}" class="btn btn-success">Ajouter un groupe</a>
    </div>
    <table class="table table-striped table-bordered table-hover">
        <thead>
        <tr>
            <th style="color: #880000;font-size: 14px">Nom du groupe</th>
            <th style="color: #880000;font-size: 14px">Option</th>
        </tr>
        </thead>
        <tbody>
        {% for group in groups %}
            <tr class="gradeX">
                <td>
                    <h6>{{ group.name|capitalize }} </h6>
                    <a href="{{ path('groups_show', { 'id': group.id }) }}" style="font-size: 9px">
                        Détail
                    </a>
                </td>
                <td>
                    <a href="{{ path('groups_edit',{'id':group.id}) }}" type="" style="margin-left: 10px">
                        <i class="fa fa-edit"></i> Modifier</a>
                    </ul>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}

